#include <stdio.h>

int main() {
    int num;
    int result;
    char op;
    char exit = 1;

    while(1) {
       



        

        printf("Exit 0, don't Exit 1 :");
            scanf("%c", &exit);

        if(exit == 0) {
                break;
            }
    }
    
}